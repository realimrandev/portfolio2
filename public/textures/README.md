# Textures

Optional folder for HDRIs and texture maps. Default scenes use Drei's built-in
`<Environment preset="forest" />` and `<Environment preset="night" />`, which
fetch HDRIs from a CDN at runtime.
